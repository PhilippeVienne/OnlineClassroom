<button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
  <span class="icon-bar"></span>
  <span class="icon-bar"></span>
  <span class="icon-bar"></span>
</button>
<a class="brand" href="#">OnlineClassroom</a>
<div class="nav-collapse collapse">
  <ul class="nav">
    <%= nav_link t('locations.home'), root_path %>
  </ul>
  <ul class="nav pull-right">
    <li class="<%= if user_signed_in? then 'dropdown' else '' end%>">
      <% if user_signed_in? then%>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <%= current_user.email %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <%= nav_link t('devise.actions.my_account'), edit_user_registration_path %>
            <%= nav_link t('groups.title'), groups_path %>
            <li>
              <%=	link_to I18n.translate('devise.actions.logout'), destroy_user_session_path, :method => :delete %>
            </li>
          </ul>
      <% else %>
          <li><%=	link_to I18n.translate('devise.actions.login'), new_user_session_path %></li>
    <% end %>
    </li>
  </ul>
</div><!--/.nav-collapse -->